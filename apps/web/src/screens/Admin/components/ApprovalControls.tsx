import{useState}from'react';import{Check,Undo2,X}from'lucide-react';import type{ApprovalStatus}from'../adminData';
export default function ApprovalControls({initialStatus,onChange,initialReason}:{initialStatus:ApprovalStatus;initialReason?:string;onChange:(s:ApprovalStatus,r?:string)=>void}){const[status,setStatus]=useState(initialStatus);const[declining,setDeclining]=useState(false);const[reason,setReason]=useState(initialReason??'');const update=(s:ApprovalStatus,r?:string)=>{setStatus(s);setDeclining(false);onChange(s,r)};return <section className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Approval</h2><p className="mt-1 text-sm text-slate-500">Current status: {status}</p></div><div className="flex gap-2"><button onClick={()=>setDeclining(true)} aria-label="Decline" title="Decline" className="rounded-full border border-red-300 p-2.5 text-red-600"><X size={19}/></button><button onClick={()=>update('Approved')} aria-label="Approve" title="Approve" className="rounded-full bg-emerald-600 p-2.5 text-white"><Check size={19}/></button></div></div>{declining&&<div className="mt-5 border-t pt-5"><label className="text-sm font-semibold">Reason for declining<textarea value={reason} onChange={e=>setReason(e.target.value)} className="mt-2 min-h-24 w-full rounded-lg border p-3"/></label><div className="mt-3 flex justify-end gap-3"><button onClick={()=>setDeclining(false)} aria-label="Cancel" title="Cancel" className="rounded-full p-2.5"><Undo2 size={19}/></button><button disabled={!reason.trim()} onClick={()=>update('Declined',reason.trim())} aria-label="Confirm decline" title="Confirm decline" className="rounded-full bg-red-600 p-2.5 text-white disabled:opacity-40"><X size={19}/></button></div></div>}</section>}
