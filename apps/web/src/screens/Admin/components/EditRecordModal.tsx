import{useState}from'react';import{Check,Undo2,X}from'lucide-react';
export interface Field{key:string;label:string;type?:string;options?:{label:string;value:string}[]}
export default function EditRecordModal({title,fields,initial,onClose,onSave}:{title:string;fields:Field[];initial:Record<string,unknown>;onClose:()=>void;onSave:(v:Record<string,string>)=>void}){const[values,setValues]=useState<Record<string,string>>(()=>Object.fromEntries(fields.map(f=>[f.key,String(initial[f.key]??'')])));const[error,setError]=useState('');const submit=(e:React.FormEvent)=>{e.preventDefault();if(fields.some(f=>f.type!=='select'&&!values[f.key]?.trim())){setError('Please complete every field.');return}onSave(values)};return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/50 p-4"><form onSubmit={submit} className="max-h-[90vh] w-full max-w-xl overflow-y-auto rounded-2xl bg-white p-7"><div className="flex justify-between"><h2 className="text-xl font-semibold">Edit {title}</h2><button type="button" onClick={onClose} aria-label="Close" title="Close" className="rounded-full p-2 hover:bg-slate-100"><X size={20}/></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{fields.map(f=><label key={f.key}><span className="text-xs font-semibold">{f.label}</span>{f.type==='select'?<select value={values[f.key]??''} onChange={e=>setValues(v=>({...v,[f.key]:e.target.value}))} className="mt-2 h-11 w-full rounded-lg border px-3">{f.options?.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select>:<input type={f.type??'text'} value={values[f.key]??''} onChange={e=>setValues(v=>({...v,[f.key]:e.target.value}))} className="mt-2 h-11 w-full rounded-lg border px-3"/>}</label>)}</div>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}<div className="mt-7 flex justify-end gap-3"><button type="button" onClick={onClose} aria-label="Cancel" title="Cancel" className="rounded-full border p-2.5"><Undo2 size={19}/></button><button aria-label="Save changes" title="Save changes" className="rounded-full bg-[#14223d] p-2.5 text-white"><Check size={19}/></button></div></form></div>}
