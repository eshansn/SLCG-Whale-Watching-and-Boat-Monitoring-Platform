import { useState } from "react";
export interface EditField { key:string; label:string; type?:"text"|"email"|"number"|"date"|"select"; options?:{label:string;value:string}[] }
export default function EditRecordModal({title,fields,initial,onClose,onSave}:{title:string;fields:EditField[];initial:object;onClose:()=>void;onSave:(v:Record<string,string>)=>void}){
 const [values,setValues]=useState<Record<string,string>>(()=>Object.fromEntries(Object.entries(initial).map(([k,v])=>[k,String(v??"")]))); const[error,setError]=useState("");
 const submit=(e:React.FormEvent)=>{e.preventDefault();if(fields.some(f=>f.key!=="boatId"&&!values[f.key]?.trim())){setError("Please complete all required fields.");return}onSave(values)};
 return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/50 p-4"><form onSubmit={submit} className="max-h-[90vh] w-full max-w-xl overflow-y-auto rounded-2xl bg-white p-7"><div className="flex justify-between"><h2 className="text-xl font-semibold">Edit {title}</h2><button type="button" onClick={onClose}>Close</button></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{fields.map(f=><label key={f.key}><span className="text-xs font-semibold">{f.label}</span>{f.type==="select"?<select value={values[f.key]??""} onChange={e=>setValues(v=>({...v,[f.key]:e.target.value}))} className="mt-2 h-11 w-full rounded-lg border px-3">{f.options?.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select>:<input type={f.type??"text"} value={values[f.key]??""} onChange={e=>setValues(v=>({...v,[f.key]:e.target.value}))} className="mt-2 h-11 w-full rounded-lg border px-3"/>}</label>)}</div>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}<div className="mt-7 flex justify-end gap-3"><button type="button" onClick={onClose} className="rounded-lg border px-5 py-2">Cancel</button><button className="rounded-lg bg-[#14223d] px-5 py-2 text-white">Save changes</button></div></form></div>
}
