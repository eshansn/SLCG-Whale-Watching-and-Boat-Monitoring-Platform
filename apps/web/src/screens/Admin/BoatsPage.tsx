import{useMemo,useState}from"react";import{Link}from"react-router-dom";import{useAdminRecords}from"./AdminRecordsContext";
export default function BoatsPage(){const[q,setQ]=useState("");const[sort,setSort]=useState("name-asc");const{boats,owners}=useAdminRecords();const rows=useMemo(()=>boats.filter(b=>`${b.name} ${b.registrationNumber} ${owners.find(o=>o.id===b.ownerId)?.name}`.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>sort==="name-desc"?b.name.localeCompare(a.name):sort==="owner"?(owners.find(o=>o.id===a.ownerId)?.name??"").localeCompare(owners.find(o=>o.id===b.ownerId)?.name??""):sort==="pending"?(a.approval==="Pending"?-1:1)-(b.approval==="Pending"?-1:1):sort==="newest"?b.registrationDate.localeCompare(a.registrationDate):a.name.localeCompare(b.name)),[q,sort,boats,owners]);return <AdminList title="Boats" subtitle="Approve, edit or delete boat registrations" search={q} setSearch={setQ} control={<Sort value={sort} onChange={setSort} options={[["name-asc","Name A–Z"],["name-desc","Name Z–A"],["owner","Owner name"],["pending","Pending first"],["newest","Newest registration"]]}/>}><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b text-xs uppercase text-slate-400"><tr><th className="py-4">Boat</th><th>Owner</th><th>Registration</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(b=><tr key={b.id} className="border-b"><td className="py-5 font-semibold">{b.name}</td><td>{owners.find(o=>o.id===b.ownerId)?.name}</td><td>{b.registrationNumber}</td><td><Status value={b.approval}/></td><td className="text-right"><Link className="font-semibold text-indigo-700" to={`/admin/boats/${b.id}`}>Manage →</Link></td></tr>)}</tbody></table></AdminList>}
export function Sort({value,onChange,options}:{value:string;onChange:(v:string)=>void;options:string[][]}){return <select value={value} onChange={e=>onChange(e.target.value)} aria-label="Sort records" className="h-11 rounded-lg border bg-white px-3 text-sm">{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select>}
export function AdminList({title,subtitle,search,setSearch,control,children}:{title:string;subtitle:string;search:string;setSearch:(s:string)=>void;control?:React.ReactNode;children:React.ReactNode}){return <main className="mx-auto max-w-7xl px-6 py-10"><section className="rounded-2xl bg-white p-7 shadow-sm"><div className="flex flex-col justify-between gap-4 lg:flex-row"><div><h1 className="text-2xl font-semibold">{title}</h1><p className="mt-1 text-sm text-slate-500">{subtitle}</p></div><div className="flex flex-col gap-3 sm:flex-row"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={`Search ${title.toLowerCase()}`} className="h-11 rounded-lg border bg-slate-50 px-4 text-sm sm:w-64"/>{control}</div></div><div className="mt-7 overflow-x-auto">{children}</div></section></main>}
export function Status({value}:{value:string}){return <span className={`rounded-full px-3 py-1 text-xs font-semibold ${value==="Approved"?"bg-emerald-50 text-emerald-700":value==="Declined"?"bg-red-50 text-red-700":"bg-amber-50 text-amber-700"}`}>{value}</span>}
