import{useMemo,useState}from"react";import{Link}from"react-router-dom";import{useAdminRecords}from"./AdminRecordsContext";import{AdminList,Sort}from"./BoatsPage";
export default function BoatOwnersPage(){const[q,setQ]=useState("");const[sort,setSort]=useState("name-asc");const{owners,boats}=useAdminRecords();const count=(id:number)=>boats.filter(b=>b.ownerId===id).length;const rows=useMemo(()=>owners.filter(o=>`${o.name} ${o.email} ${o.nic}`.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>sort==="name-desc"?b.name.localeCompare(a.name):sort==="boats-desc"?count(b.id)-count(a.id):sort==="boats-asc"?count(a.id)-count(b.id):a.name.localeCompare(b.name)),[q,sort,owners,boats]);return <AdminList title="Boat owners" subtitle="Edit, delete and view boats registered under each owner" search={q} setSearch={setQ} control={<Sort value={sort} onChange={setSort} options={[["name-asc","Name A–Z"],["name-desc","Name Z–A"],["boats-desc","Most boats"],["boats-asc","Fewest boats"]]}/>}><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b text-xs uppercase text-slate-400"><tr><th className="py-4">Owner</th><th>NIC</th><th>Phone</th><th>Boats</th><th></th></tr></thead><tbody>{rows.map(o=><tr key={o.id} className="border-b"><td className="py-5"><p className="font-semibold">{o.name}</p><p className="text-xs text-slate-500">{o.email}</p></td><td>{o.nic}</td><td>{o.phone}</td><td>{count(o.id)}</td><td className="text-right"><Link className="font-semibold text-indigo-700" to={`/admin/owners/${o.id}`}>Manage →</Link></td></tr>)}</tbody></table></AdminList>}
